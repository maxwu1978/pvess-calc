#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_ROOT"

if [[ -x "$REPO_ROOT/.venv/bin/pvess" ]]; then
  exec "$REPO_ROOT/.venv/bin/pvess" visual-iterate "$@"
fi

exec pvess visual-iterate "$@"
